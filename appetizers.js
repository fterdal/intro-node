const tastyAppetizers = [
  "jalapeño poppers",
  "grilled calamari",
  "mozz sticks",
  "chicken liver paté"
];

const formatAppetizers = (apps) => {
  return apps.join(", ")
}

module.exports = {
  tastyAppetizers,
  formatAppetizers
